# adk-mcp-kaggle.zip
Self-extending agent that turns natural-language tasks into runnable tools on demand. It derives a formal spec (name, I/O schema, pre/postconditions), synthesizes Python/TypeScript code, executes tests in a sandbox
